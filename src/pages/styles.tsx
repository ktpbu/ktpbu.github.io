import styled from 'styled-components';

export const Header = styled.div`
    font-size: 48px;
    color: #234c8a;
    font-weight: bold;
    margin-bottom: 16px;
`;

export const BodyText = styled.div`
    font-size: 16px;
    font-weight: 600;
    color: var(--black);
`